---
tags: [decision, tech-debt, snapshot]
date: 2026-04-27
status: open
---

# Технический долг — снапшот 2026-04-27

Сводная страница. Полный анализ — в `.planning/codebase/CONCERNS.md`.

## 5 пунктов, которые ждут работы

| # | Пункт | Где | Почему чинить |
|---|---|---|---|
| 1 | `state/sent_signals.json` без ротации (3.4 МБ, 108k записей) | `state.py` | Растёт без верхней границы; на старте `load_state` грузит весь JSON в память, при загрузке несколько секунд блокирует. |
| 2 | `requirements.txt` без версий | `requirements.txt` | Нерепродуцируемая среда: `pip install -r` поставит свежий pandas/websockets, поведение детекторов не гарантировано. |
| 3 | Sync `requests` в async-приложении | `telegram_bot.py`, остальные сетевые вызовы | Блокирует event-loop при медленном Telegram → задерживает обработку WS-свечей. |
| 4 | Нет обработки rate-limit Telegram | `telegram_bot.py` | При лавине broadcast или 429 от API нет backoff/retry, сигналы могут потеряться. |
| 5 | `signals_report.html` (23 МБ) исторически попадал в репо | repo root | На текущий момент в `.gitignore` и не отслеживается, но артефакт всё ещё генерируется в worktree — стоит вынести в `signals/` и/или удалить. |

## Что НЕ в этом списке

- Расхождения vault ↔ код — закрыты сессией 2026-04-27 (см. session note этой даты).
- Архивные решения D-09..D-12 — частично пересмотрены ([[bootstrap-sync-hard-exit]]),
  частично актуальны ([[zone-lifecycle-no-ttl]]).

## Источник истины

`.planning/codebase/CONCERNS.md` — здесь только сводка. Не дублировать содержимое.

## Связано

- [[архитектура проекта flat layout]]
- [[bootstrap-sync-hard-exit]]
