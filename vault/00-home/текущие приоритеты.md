---
tags: [priorities]
date: 2026-04-23
---

# Текущие приоритеты

## Фаза

**Phase 1 — s1 OBx4 + OB1h: PLANNED ✅** (2026-04-23). Всё готово к `/gsd-execute-phase 1`.

- CONTEXT.md (D-01..D-12), RESEARCH.md (16 Q&A), VALIDATION.md (Nyquist OK), 8 PLAN.md в 4 волнах.
- Plan-checker: **VERIFICATION PASSED** (0 BLOCKERs, 5 WARNINGs некритичные).

## Задача на ближайшую сессию

1. `/gsd-resume-work` (подхватить HANDOFF + .continue-here.md).
2. `/gsd-execute-phase 1` — запустит Wave 1 (3 плана параллельно).
3. Мониторить за 4 моментами (см. .continue-here.md):
   - **PLAN 01 task 01.1** — `occurred_at = c5_time`, не c1_time (RESEARCH §Q1).
   - **PLAN 03 task 03.2** — grep `"901107007"` должен найти 4 места и убрать кроме test fixture.
   - **PLAN 06** — какое имя у TelegramClient.send-метода (executor должен прочитать `src/bot/telegram_client.py`).
   - **PLAN 04** — `_set_status` решение по frozen vs mutable Zone.

## Контекст для Claude Code

- Phase 0 wiring **уже подготовил** Phase 1: `orchestrator.py:202-209` имеет цикл по стратегиям
  для `on_htf_close`. Phase 1 добавляет: `_dispatch_signal` helper + `check_active_zones`
  dispatch на 1h + `lookback_rebuild` для D-04.
- **Миграция 1-в-1 из `reference/obxxx.py`** для `detect_obx4_*` и `find_ob1h_in_obx4_zone`
  (lines 760-867 и 960-1066). Не оптимизировать, не добавлять «smart-фильтры».
- 17 задач в 4 волнах. Critical path: PLAN 02 → 04 → 06 → 07 → 08.
- Один task non-autonomous: **PLAN 08 task 08.3** (manual snapshot, требует `data/BTCUSDT_*.csv`).

## Новые архитектурные решения этой сессии

- [[bootstrap-sync-hard-exit]] — D-11: sync bootstrap order, hard exit при ошибке rebuild.
- [[zone-lifecycle-no-ttl]] — D-09/D-10: без TTL, параллельные зоны на разных HTF независимы.

## Что НЕ делать сейчас

- Не начинать s2–s5 (Phase 2-3).
- Не трогать `neural/` (Phase 5).
- Не менять `detect_obx4_*` логику — миграция 1-в-1.
- Не реализовывать TTL/EXPIRED для зон — отложено.
- Не реализовывать `--backfill-signals` — отложено в Phase 4.

## Состояние проекта

- Ветка: `main`, ~14 коммитов впереди `origin/main` (Pavel push'ит сам).
- Последний коммит: `22e93e3` (WIP handoff — Phase 1 paused at planned).
- HANDOFF: `.planning/HANDOFF.json` + `.planning/phases/01-obx4-ob1h-2/.continue-here.md`.
- Сессия: [[phase-1-planned-2026-04-23]].

## Human actions pending (non-blocking)

- Обновить `.env`: `ADMIN_IDS=886807304` (вручную, .env в .gitignore).
- Опционально: бот → админ канала `@ASVK_Power_Zone` (или ЛС через `users.json`).
- Перед PLAN 08 task 08.3 убедиться, что `data/BTCUSDT_*.csv` забутстрапированы.

## MCP

- Context7 — для актуальных API pandas/Binance/Telegram.
- Filesystem — доступ к `vault/`, `data/`, `reference/`.
