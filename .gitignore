# Окружение
venv/
.env
__pycache__/
*.pyc
.DS_Store

# Данные — автогенерируются при первом запуске
data/
state/
signals/

# HTML-отчёт — автогенерируется через generate_report.py
signals_report.html
dashboard.html

# state artifacts (на всякий случай, хотя state/ уже игнорится выше)
state/admins.json
state/bot.log
state/bot.log.1
state/signals_today.json

# Логи
*.log
# GSD locally-installed config (each developer installs separately)
.claude/*
# но project-specific агенты шарим через git
!.claude/agents/
.claude/agents/*
!.claude/agents/spec-syncer.md
!.claude/agents/backtest-auditor.md
!.claude/agents/pitfall-checker.md
!.claude/agents/smc-reviewer.md

# Obsidian — личные настройки разработчика
vault/.obsidian/workspace
vault/.obsidian/workspace.json
vault/.obsidian/workspace-mobile.json
vault/.obsidian/cache
vault/.obsidian/graph.json
.DS_Store
